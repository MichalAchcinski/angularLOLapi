import { ObjectivesFromGameSummary } from './objectives-from-game-summary';

describe('ObjectivesFromGameSummary', () => {
  it('should create an instance', () => {
    expect(new ObjectivesFromGameSummary()).toBeTruthy();
  });
});
