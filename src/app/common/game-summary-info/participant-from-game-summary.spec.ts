import { ParticipantFromGameSummary } from './participant-from-game-summary';

describe('ParticipantFromGameSummary', () => {
  it('should create an instance', () => {
    expect(new ParticipantFromGameSummary()).toBeTruthy();
  });
});
