import { BansFromGameSummary } from './bans-from-game-summary';

describe('BansFromGameSummary', () => {
  it('should create an instance', () => {
    expect(new BansFromGameSummary()).toBeTruthy();
  });
});
